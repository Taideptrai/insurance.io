export default {
  isLoading({ isLoading }) {
    return isLoading
  },
  isSidebarVisible({ sidebarVisible }) {
    return sidebarVisible
  },
  title({ title }) {
    return title
  },
  backRouter({ backRouter }) {
    return backRouter
  }
}
